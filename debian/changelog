adaptation-samsung-gts7xlwifi (0.0.11) trixie; urgency=medium

  * configs: add Replaces/Breaks on bluez so /etc/bluetooth/main.conf can be
    shipped. bluez 5.82+ now provides that file, causing a dpkg --unpack
    overwrite conflict that broke the Droidian rootfs build.

 -- tarpediem <omagnier@gmail.com>  Sun, 12 Jul 2026 08:40:00 +0000

adaptation-samsung-gts7xlwifi (0.0.10) trixie; urgency=medium

  * Bluetooth: pin adapter Class of Device to Computer/Laptop (0x00010c) via
    etc/bluetooth/main.conf. Stock left the CoD as "Miscellaneous", so peer
    devices (phones) saw the tablet as an audio sink (music-note/headphone icon)
    and would not offer file transfer. The BT stack itself (adapter, pairing,
    A2DP audio) is fixed in the kernel (hci_sock.c un-gut + rfcomm/bnep/hidp
    Makefile subdirs) and needs no adaptation change. OBEX file-receive
    (bluez-obexd + an auto-accept session agent) remains a documented follow-up.

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Mon, 01 Jun 2026 00:06:50 +0300

adaptation-samsung-gts7xlwifi (0.0.9+git20260531152506.0000000.trixie) trixie; urgency=medium

  * Camera: fix the 90-degree clockwise preview rotation (both front + back).
    The camera HAL already delivers upright frames; droidian-camera's Qt
    VideoOutput.autoOrientation was adding a spurious 90deg on this
    landscape-native tablet. Ship:
    - usr/lib/droidian/device/droidian-camera.conf (primary config path, wins
      over /etc): backend=aal, autoorientation=false, orientation=0.
    - etc/apt/sources.list.d/mukahraman-droidian-camera.list + a preferences.d
      pin: pull the patched droidian-camera (mukahraman fork) that adds the
      orientation/autoorientation config keys the conf above relies on.

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Sun, 31 May 2026 15:25:06 +0300

adaptation-samsung-gts7xlwifi (0.0.8+git20260530192000.0000000.trixie) trixie; urgency=medium

  * Display: fix newly-launched apps not appearing until a screen lock/unlock. phoc on the
    hwcomposer backend (command-mode DSI panel) direct-scans-out a fullscreen surface and does
    not recomposite when a new window maps on top, leaving the screen stale until a forced
    repaint. Ship etc/environment.d/65-phoc-no-direct-scanout.conf setting
    WLR_SCENE_DISABLE_DIRECT_SCANOUT=1 so phoc always composites and new windows render
    immediately. Confirmed: apps now pop up right away.

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Sat, 30 May 2026 19:20:00 +0000

adaptation-samsung-gts7xlwifi (0.0.7+git20260530183000.0000000.trixie) trixie; urgency=medium

  * S-Pen: make the Wacom digitizer (sec_e-pen / W9021) work as a stylus. libinput was
    ignoring it ("missing tablet capabilities: resolution") because the Samsung Android
    driver leaves ABS axis resolution unset. Ship etc/libinput/local-overrides.quirks with
    AttrResolutionHint=100x100 (26712x16714 over the ~267x167mm panel) so libinput treats it
    as a tablet, plus etc/udev/rules.d/99-epen-calib.rules applying the same 90-deg
    LIBINPUT_CALIBRATION_MATRIX as the touchscreen (the pen reports in panel-native portrait
    orientation). Confirmed: pen draws with pressure in Xournal++ (the native apt build --
    the Flatpak's bundled Mesa can't drive the Adreno GPU, so install graphical apps via apt).

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Sat, 30 May 2026 18:30:00 +0000

adaptation-samsung-gts7xlwifi (0.0.6+git20260530164500.0000000.trixie) trixie; urgency=medium

  * Wi-Fi: make route-install reliable across boots (systemd service + shared helper).
    The NM dispatcher alone didn't reliably fire at boot. Add wlan-routes.service
    (oneshot, After=NetworkManager) running /usr/lib/adaptation-samsung-gts7xlwifi/
    wlan-routes-fix: a self-contained ~90s-retry helper that reads the address off the
    live Wi-Fi interface (the name varies swlan0/wlp1s0/wlanN across boots) and installs
    the network + default routes, which NM leaves unset (empty IP4.GATEWAY +
    noprefixroute). The 50-wlan-routes dispatcher now calls the same helper on
    up/dhcp4-change for mid-session reconnects. Service enabled via debian/links.
  * Firefox: force H.264 + cap fps for usable YouTube. Add usr/lib/firefox/defaults/
    pref/zz-halium-video-gts7xlwifi.js: disable AV1/VP9/WebM (no GPU/VAAPI video decode
    on this Halium device, so VP9/AV1 software-decode lagged; H.264 is ~4-6x cheaper)
    and set layout.frame_rate=60 to ease fullscreen compositing on the 120Hz panel.

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Sat, 30 May 2026 16:45:00 +0000

adaptation-samsung-gts7xlwifi (0.0.5+git20260530075616.0000000.trixie) trixie; urgency=medium

  * Wi-Fi: add NetworkManager dispatcher script to install routes on connect.
    On this device NM gets the DHCP lease (IP + DNS + 'routers' option) but fails
    to install the connected route or the default route (IP4.GATEWAY ends up
    empty) -> "connected but no internet". etc/NetworkManager/dispatcher.d/
    50-wlan-routes reinstalls them from the NM-exported env on each wlp1s0 up /
    dhcp4-change. Non-disruptive (does not toggle the interface — toggling WLAN
    via nmcli down/up wedges the cnss driver on this device).

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Sat, 30 May 2026 07:56:16 +0000

adaptation-samsung-gts7xlwifi (0.0.4+git20260529234841.0000000.trixie) trixie; urgency=medium

  * Auto-rotate: enable working automatic screen rotation (display + touch).
    - debian/postinst: set sensorfw accelerometer transformation_matrix to
      "0,-1,0,1,0,0,0,0,1" in /etc/sensorfw/sensord-hybris.conf so the reported
      orientation matches the physical device in all four orientations (the raw
      hybris accel axes were swapped + one inverted relative to the panel).
    - Enable sensorfwd.service (provides the accelerometer data; iio-sensor-proxy
      is D-Bus-activated on top of it).
    - Stop masking iio-sensor-proxy.service (revert the A3 landscape-lock from
      0.0.3) so phosh receives orientation events and rotates.
    The landscape touch calibration matrix (98-touch-calib.rules) is kept: it is
    the base digitizer<->panel alignment, and the compositor rotates touch on top
    of it, so touch follows the display correctly in every orientation.
  * Verified on-device: display + touch both rotate correctly in all orientations.

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Fri, 29 May 2026 23:48:41 +0000

adaptation-samsung-gts7xlwifi (0.0.3+git20260529225627.0000000.trixie) trixie; urgency=medium

  * Display/input/power fixes (fold in manual bring-up tweaks):
    - etc/udev/rules.d/98-touch-calib.rules: set LIBINPUT_CALIBRATION_MATRIX
      "0 1 0 -1 0 1" on sec_touchscreen so touch matches the locked-landscape
      phosh output.
    - Mask iio-sensor-proxy.service: keep the display in landscape so the fixed
      touch matrix above stays correct (no auto-rotate flipping it).
    - Mask suspend.target / sleep.target / hibernate.target / hybrid-sleep.target
      / suspend-then-hibernate.target: idle-suspend cannot resume on this device
      and leaves it black/frozen, so block sleep entirely until resume works.

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Fri, 29 May 2026 22:56:27 +0000

adaptation-samsung-gts7xlwifi (0.0.2+git20260529223443.0000000.trixie) trixie; urgency=medium

  * Audio: bake in the working speaker bring-up for gts7xlwifi.
    - etc/pulse/arm_droid_card_custom.pa: load module-droid-card with
      use_legacy_stream_set_parameters=true. This avoids the create_audio_patch
      null-function-pointer path in the Halium audio.hidl_compat wrapper, which
      otherwise hard-wedges userspace when the droid card initialises.
    - usr/lib/droid-vendor-overlay/etc/init/android.hardware.audio.service.rc:
      re-enable the vendor.audio-hal HIDL service (the android image ships it
      disabled via init.disabled.rc); run it as root with the audio HAL groups
      and BLOCK_SUSPEND capability. The /vendor overlay 'override' keyword
      replaces the disabled definition.
    - usr/lib/droid-vendor-overlay/lib64/hw/audio.primary.kona.so: the 64-bit
      Halium audio.hidl_compat wrapper, installed under the audio.primary.<hw>
      name so the 64-bit pulseaudio module-droid-card loads it and bridges over
      hwbinder to the 32-bit kona audio HAL (solves the 64-bit-userspace /
      32-bit-only-HAL wall).
  * Verified on-device: pulseaudio creates droid_card.primary + sink.primary-out;
    a test tone is audible on the CS35L41 speakers.

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Fri, 29 May 2026 22:34:43 +0000

adaptation-samsung-gts7xlwifi (0.0.1+git20260527125500.0000000.trixie) trixie; urgency=medium

  * Initial adaptation package for Samsung Galaxy Tab S7+ Wi-Fi (SM-T970,
    codename gts7xlwifi), forked from ArchieMeng's adaptation-sony-pdx206.
  * Rename all sony-pdx206 paths and identifiers to samsung-gts7xlwifi.
  * Rename udev rules file 70-pdx206.rules -> 70-gts7xlwifi.rules (contents
    are generic Halium/Qualcomm sm8250 permissions; no device-specific
    binding required at this stage).
  * Tune etc/phosh/phoc.ini for the 12.4" 2800x1752 tablet panel.
  * Remove ArchieMeng's apt-repo GPG keyring (will be replaced by a
    mukahraman-signed key once our own apt repo is wired up).
  * Stop blanket-ignoring debian/ in .gitignore so future package files
    don't silently disappear.

 -- Mustafa Kahraman <mkahraman341@gmail.com>  Wed, 27 May 2026 12:55:00 +0000
